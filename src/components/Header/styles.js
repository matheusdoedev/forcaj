import styled from "styled-components";
import colors from "../../styles/settings/colors";

export const HeaderWrapper = styled.header`
  padding: var(--gap-md) 0;
  background: ${colors.primary.color2};
`;
